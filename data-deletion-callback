<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Deletion Callback - Gava Tana</title>
</head>
<body>
    <h1>Data Deletion Callback Endpoint</h1>
    <p>This endpoint handles automated data deletion requests from Facebook.</p>
    <p>Status: Active</p>
    <p>For manual data deletion requests, please visit <a href="data-deletion">/data-deletion</a></p>

    <?php
    // This PHP section would handle POST requests from Facebook
    if ($_SERVER['REQUEST_METHOD'] === 'POST') {
        $input = json_decode(file_get_contents('php://input'), true);
        
        // Log the request
        error_log('Data deletion callback received: ' . json_encode($input));
        
        // Process deletion based on user_id
        // In production, this would queue a deletion job
        
        // Return confirmation as required by Facebook
        header('Content-Type: application/json');
        echo json_encode([
            'url' => 'https://demo-one-zeta-92.vercel.app/data-deletion-status',
            'confirmation_code' => uniqid('del_')
        ]);
        exit();
    }
    ?>
</body>
</html>